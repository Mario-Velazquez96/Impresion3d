# Implementation log — 11_image_prep

Implementer session 2026-07-17. Spec approved (`in_progress`). All tasks in
`specs/11_image_prep/tasks.md` executed in order and checked off.

## Status: COMPLETE — all checks green

- `corepack pnpm typecheck` — clean (0 errors).
- `corepack pnpm lint` — 0 errors, 0 new warnings (the one warning my hook
  introduced — `react-hooks/exhaustive-deps` on a ref in effect cleanup — was
  fixed by capturing the stable Map instance inside the effect; the remaining
  4 warnings are pre-existing in `components/planning/__tests__/WeekPlanner.test.tsx`).
- `corepack pnpm test` (vitest + coverage) — **847 tests / 61 files, all
  passing** (was 761/57 before this feature → **+86 tests / +4 files**).
- **`lib/image-prep-core.ts`: 100% statements / 100% branch / 100% funcs /
  100% lines** (the tasks.md hard target).
- Other changed modules (target ≥ 80% lines): worker-messages.ts 100,
  useImagePrepWorker.ts 100, decode.ts 100, PosterizePanel.tsx 100,
  HistogramChart.tsx 100, PalettePanel.tsx 98.7, ImagePrep.tsx 95.1,
  ImageDropzone.tsx 93.0, AdjustPanel.tsx 92.5, BeforeAfterPreview.tsx 90.5,
  MainNav.tsx 100. (`types.ts` is type-only — zero executable lines;
  `app/(app)/image-prep/page.tsx` is a thin Server Component outside the
  repo's vitest coverage include, same as the calculator page — both are
  E2E-covered.)
- `pnpm build` deliberately NOT run per leader instruction (shared `.next`
  corruption gotcha while a dev server may be running — see
  `progress/current.md`). Typecheck/lint/test stand in.
- E2E `e2e/image-prep.spec.ts` written but not executed — credential-gated
  (`E2E_EMPLOYEE_*`), matching every existing suite (e.g. `e2e/calculator.spec.ts`).

## Files created

- `lib/image-prep-core.ts` — pure core: constants, color math (hex/HSL/Rec.601/
  redmean), fitWithin/downloadFileName/formatByteSize, adjustment LUT +
  saturation + auto-levels + histogram, deterministic median-cut,
  nearestIndex, mapToPalette (flat + Floyd–Steinberg), quantize,
  coveragePercent, classifyPalette, mergeEntries/mergeSimilar/mergeTiny,
  snapToCatalog, indexedToPixels. No DOM/Prisma/React/server-only.
- `components/image-prep/worker-messages.ts` — typed protocol + the two
  (de)serialization helpers for `IndexedImage`.
- `components/image-prep/image-prep.worker.ts` — stateless, logic-free worker
  dispatcher (coverage-excluded, see vitest note below).
- `components/image-prep/useImagePrepWorker.ts` — lazy worker, Promise-per-id,
  busy flag, terminate on unmount.
- `components/image-prep/types.ts` — `ColorView`, `LoadedImageInfo`.
- `components/image-prep/decode.ts` — DOM decode glue (createImageBitmap →
  white-filled canvas → PixelBuffer, downscale via fitWithin) + the
  `ACCEPTED_IMAGE_TYPES` allow-list.
- `components/image-prep/ImageDropzone.tsx`, `AdjustPanel.tsx`,
  `HistogramChart.tsx` (SVG bars), `PosterizePanel.tsx`, `PalettePanel.tsx`,
  `BeforeAfterPreview.tsx`, `ImagePrep.tsx` (the island with the Stage union).
- `app/(app)/image-prep/page.tsx` — `requireUser()` (no admin gate) + ONE
  `db.color.findMany` + `<ImagePrep/>`; metadata title per spec.
- Tests: `lib/__tests__/image-prep-core.test.ts` (57 tests),
  `components/image-prep/__tests__/ImagePrep.test.tsx` (18),
  `components/image-prep/__tests__/useImagePrepWorker.test.ts` (5),
  `components/image-prep/__tests__/decode.test.ts` (4).
- `e2e/fixtures/image-prep-sample.png` — 64×64, four solid blocks
  (red/blue/white/black), 140 bytes, generated by a throwaway script.
- `e2e/image-prep.spec.ts` — signed-out redirect + nav link + full
  upload→apply→posterize→snap→download flow (real worker + canvas decode).

## Files modified

- `components/layout/MainNav.tsx` — "Image prep" link after Finances, OUTSIDE
  the `showAdmin` block (+ doc comment updated).
- `components/layout/__tests__/MainNav.test.tsx` — `it.each([false, true])`
  for the Image prep link (R1).
- `vitest.config.ts` — the ONE authorized config edit: coverage exclude for
  `components/image-prep/image-prep.worker.ts` with its one-line reason.

## No-persistence contract (R19) — verified via `git status` / `git diff`

`prisma/schema.prisma` and `prisma/migrations/` untouched; no `actions/` file,
no `app/api/` route, no Storage code, no `.env.example` entry, no
`package.json` change (zero new dependencies); no `localStorage`/cookie/URL
state anywhere in the feature. (`feature_list.json` and `progress/current.md`
were already modified by the leader before this session; not touched here.)

## Decisions / deviations (all minor, none change spec behavior)

1. **`mapToPalette(src, palette, dither)` exported** in addition to the
   spec-listed `quantize`. Reason: the tasks.md test "a hand-computed
   Floyd–Steinberg case (2×2 grey vs black/white palette)" is mathematically
   impossible through `quantize` alone — a median-cut palette derived from an
   image containing grey can never be exactly {black, white} (box means
   include the grey). `quantize` is now the trivial composition
   `mapToPalette(src, medianCutPalette(src, n), dither)`. Pure superset; no
   behavior change.
2. **Validation + decode invocation live in `ImageDropzone`** (design.md
   sketched the decode call in the island). Keeps the `role="alert"` error
   region single-sourced (type/size/decode failures all report in one place)
   and automatically leaves island state untouched on failure (R3). The
   island still owns all pipeline state; decode glue itself is the separate
   mockable `decode.ts` module.
3. **`worker-messages.ts` carries the two tiny (de)serialize helpers** (design
   said "types only"). Both the worker and the island need them; sharing
   prevents wire-shape drift. Fully unit-covered (100%).
4. **Classify-boundary test is near-threshold, not exactly-at-threshold**: an
   HSL saturation of exactly 0.12 is not representable in float from 8-bit
   channels (every s is a ratio of /255 fractions), so the strict-`<`
   comparison is pinned by the nearest representable neighbors
   (s≈0.102 → neutral, s≈0.124 → color) with a comment explaining why.
5. **Posterize is allowed straight from the `loaded` stage** (adjust is
   optional per the requirements' "Adjust (optional…)"): the original then
   serves as the adjusted source and the histogram slot is null until the
   first Apply. R7's "quantize the adjusted image" is preserved — after any
   Apply, posterize always consumes the adjusted buffer.
6. Extra tests beyond tasks.md: hook tests against a stubbed global `Worker`
   and decode tests against stubbed `createImageBitmap`/canvas — added to hit
   the ≥80%-lines target on those changed modules (they'd otherwise be
   covered only by E2E).

## Requirements → tests (traceability)

- **R1** → MainNav.test.tsx "Image prep link … showAdmin %s"; e2e/image-prep.spec.ts redirect + nav tests.
- **R2** → core "io edges"/"color math"; ImagePrep.test "shows dimensions and formatted size"; E2E upload step.
- **R3** → ImagePrep.test "rejects a non-image type…", "rejects an oversize file…", "surfaces a decode failure…".
- **R4** → core "fitWithin downscales…"; decode.test "downscales oversized…"; ImagePrep.test "shows the downscale notice…".
- **R5** → core "adjustments" + "histogram + auto-levels" suites; ImagePrep.test "recomputes ONLY on Apply…"; E2E Apply step.
- **R6** → core histogram tests; ImagePrep.test histogram assertions (renders after Apply).
- **R7** → core "median cut" suite (k≤n, clusters, clamps, determinism, tie-breaks) + quantize tests; ImagePrep.test posterize suite; E2E posterize step.
- **R8** → core "matches the hand-computed 2×2 Floyd–Steinberg…", "dither off maps flat…"; ImagePrep.test "…dithering off by default", "passes a changed color count and dither flag".
- **R9** → core "palette stats + classification"; ImagePrep.test "renders the classified palette with coverage"; E2E 25.0% assertions.
- **R10** → core mergeEntries tests; ImagePrep.test "tap A then B merges…deselects without merging".
- **R11** → core mergeSimilar tests (incl. strict threshold + tie-breaks); ImagePrep.test "Merge similar … send their thresholds".
- **R12** → core mergeTiny tests (smallest-first, stop-at-one, strict boundary); ImagePrep.test same test as R11.
- **R13** → core snapToCatalog tests; ImagePrep.test "snaps every entry to a filament…"; E2E snap step.
- **R14** → core "empty catalog returns the input unchanged"; ImagePrep.test "disables snapping with an explanatory note…".
- **R15** → core indexedToPixels test; ImagePrep.test pipeline tests (preview follows newest stage); BeforeAfterPreview rendered in every loaded test.
- **R16** → ImagePrep.test "re-applying adjustments discards the quantized palette", "loading a new file resets the whole pipeline".
- **R17** → core downloadFileName tests; ImagePrep.test "Download PNG names the file <base>-prepped.png with no network request"; E2E download event.
- **R18** → ImagePrep.test "disables the action controls while the worker is busy"; useImagePrepWorker.test (busy lifecycle, rejection, teardown); E2E (real worker).
- **R19** → git-status contract check above; ImagePrep.test fetch-spy assertion; E2E reload-resets step.

## For the reviewer

- Feature NOT marked done in `feature_list.json` (reviewer gate).
- Suggested manual smoke: `corepack pnpm dev` → sign in → /image-prep →
  drop any photo → Apply → Posterize → merge/snap → Download.
